services:
  fluentd_service:
    image: josenerydev/fluentd:latest
    volumes:
      - ./conf:/fluentd/etc
    ports:
      - "24224:24224"
      - "24224:24224/udp"
    networks:
      - elastic_network
    deploy:
      replicas: 1

networks:
  elastic_network:
    external: true
