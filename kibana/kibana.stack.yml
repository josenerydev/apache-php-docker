services:
  kibana:
    image: kibana:8.15.3
    ports:
      - "5601:5601"
    networks:
      - elastic_network
    volumes:
      - ./kibana.yml:/usr/share/kibana/config/kibana.yml
    deploy:
      restart_policy:
        condition: on-failure
networks:
  elastic_network:
    external: true
